# CRUD Productos — SWITCH & VALIDADO
- Preferencia por **switch** en JS y PHP (incluye `switch(true)` para guard clauses).
- `registrar.php` centraliza crear/editar sin alterar la API (`ok|modificado|sin_cambios|error:`).
- Validaciones fuerte: HTML5 + JS + PHP. Duplicado de `codigo` bloqueado.
- Eliminar con confirmación y refresco. Búsqueda segura con placeholders.
